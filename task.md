<!-- - Создайте проект ManipulationStrings, целью которого является чтение входной строки и вывод длины входной строки, верхнего регистра строки, нижнего регистра строки, первого символа строки, двух последних символов строки.
Шаги:
- После создания инициализируйте Git-репозиторий;
- переименуйте класс App в StringManipulation ;
- добавьте все в папку проекта;
- зафиксируйте с комментарием 'initial commit';
- в зависимости от первой функциональности, которую вы хотите добавить, создайте связанные ветки от основной ветки;
- в зависимости от первой функциональности, которую вы решите развивать, создайте статический метод в классе StringManipulation, который принимает строку в качестве аргумента и возвращает ожидаемый результат;
- вызовите созданный метод в главном методе и отобразите его вывод, передав в качестве входных данных считанную строку; 
- добавьте измененные файлы и зафиксируйте их с "соответствующим" комментарием;
- вернитесь на master и объедините ветку, над которой вы только что работали;
- повторите операцию для оставшихся математических операций, чтобы завершить первоначальный запрос;
- создайте репозиторий на github и выполните push:
- удалите созданные ветки локально.

- Creare un progetto denominato ManipulazioneStringhe il cui scopo e leggere una stringa in input e fare output della lunghezza della stringa inserita, la stringa in maiuscolo, la stringa in minuscolo, il primo carattere della stringa, gli ultimi due caratteri della stringa.
Passi:
- Una volta creato, inizializzarvi il repository Git;
- rinominare la classe App in ManipulazioneStringhe ;
- fare add di tutto cio che e presente nella folder del progetto;
- fare commit con commento "commit iniziale";
- in base alla prima funzionalita che si vuole aggiungere, creare branch relativo a partire dal branch master;
- in base alla prima funzionalita che decidete sviluppare, creare nella classe ManipulazioneStringhe un metodo statico che riceve una stringa come parametro e ne restituisca il risultato atteso;
- richiamare il metodo appena creato nel metodo main e visualizzarne l`output passando in input la stringa proveniente da lettura; 
- fare add dei file modificati e fare commit con commento "adeguato";
- tornare su master e fare merge del branch appena lavorato;
- ripetere l`operazione per le restanti operazioni matematiche utili a concludere la richiesta iniziale;
- creare un repository su github e fare push:
- cancellare localmente i branch creati. -->